export interface SkillType {
  id: string,
  name: string,
  icon: string,
  date: string
}

export interface ProjectType {
  id: string,
  name: string,
  year: string,
  desc: string,
  tags: string[],
  link: string
}
